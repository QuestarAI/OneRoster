namespace Questar.OneRoster.Client.Internals
{
    using Implementations;
    using Models;

    public class ResultEndpoint : EditEndpoint<Result>
    {
        public ResultEndpoint(string path) : base(path)
        {
        }
    }
}